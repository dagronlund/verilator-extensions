[package]
name = "simulator-rust"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
parser-verilator = { path = "../parser-verilator" }

[lints]
workspace = true
