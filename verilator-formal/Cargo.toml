[package]
name = "verilator-formal"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
ron = "0.12"
verilator-utils = { path = "../verilator-utils" }
verilator-parser = { path = "../verilator-parser" }

[lints]
workspace = true
