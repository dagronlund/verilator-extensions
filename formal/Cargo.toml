[package]
name = "formal"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
ron = "0.12"
formal-utils = { path = "../formal-utils" }
parser-verilator = { path = "../parser-verilator" }

[lints]
workspace = true
